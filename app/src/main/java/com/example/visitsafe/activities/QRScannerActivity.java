package com.example.visitsafe.activities;

import com.journeyapps.barcodescanner.CaptureActivity;

public class QRScannerActivity extends CaptureActivity {
    // This class is required for ZXing to customize the scanning activity if needed
    // For now, it just inherits the default behavior
}